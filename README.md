# Octagon-Data-Filtering
Backend application/API that generates organizational charts from employee data, built with Python/Flask, MongoDB, and python-pptx. My contribution: implemented the data filtering layer — querying and processing employee records from MongoDB to structure them for chart generation.
